name: Monitoring Gate

on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  monitoring-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate monitoring and alerting configuration
        run: bash ./scripts/monitoring_config_check.sh
