{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -lc 'case \"${BACKEND_ROLE:-api}\" in api) exec ./api ;; crawler) echo STARTING_CRAWLER; CRAWLER_HEALTH_ADDR=:${PORT} exec ./crawler ;; notifier) NOTIFIER_HEALTH_ADDR=:${PORT} exec ./notifier ;; *) echo \"unknown BACKEND_ROLE=${BACKEND_ROLE}\" >&2; exit 1 ;; esac'",
    "healthcheckPath": "/healthz",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
